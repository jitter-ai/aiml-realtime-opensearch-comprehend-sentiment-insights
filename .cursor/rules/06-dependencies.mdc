---
description: Dependency and version management — PyPI, requirements.txt
globs: "**/*.py,**/requirements*.txt"
alwaysApply: false
---

# Dependency and Version Management

- Always verify latest PyPI versions before importing new libraries.
- Maintain a single canonical `requirements.txt`.
- Every dependency change must include a brief comment explaining why.
- If a `requirements.txt` is missing, create it automatically.
